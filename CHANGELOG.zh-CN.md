# 更新日志

这里只记录用户可见能力、兼容性和安全边界。实现过程、测试数量和临时报告不重复写入。

## 3.0.1

- 扩展受控修改：`etc/titlebook.etc` 的 `[send postal]` 支持路径受限的完整中文邮件文字；同一 PVF 内的普通文本文件可复制到不存在的同扩展名路径；整数与小数字段可用“完整标签 + 精确原值”互改。其他路径、覆盖目标、高风险类型和裸重复数字仍会被阻止。
- 既有 `sqr/*.nut` 增加 ASCII 专用路线。工作台会绑定完整原文、既有加载链、目标 API、函数和 APID，只替换唯一定位的 ASCII 原始字节，并在临时输出与最终输出中复核文本和原始字节；注释不能伪造证据，非目标字节必须保持不变。中文、StringLink 和普通既有高风险文件仍不开放，静态通过后仍需实机验证。
- 修复以测试客户端现有 `Script.pvf` 为修改来源时无法受控部署的问题。已核对的内容寻址备份可成为受保护锚点，后续累计修改继续绑定该锚点；客户端基线不符、备份不完整、输出冲突或客户端未确认关闭时仍会停止。

## 3.0.0

- 重整 Agent 冷启动和指令归属：根规则只保留第一命令、全局边界和短路由，具体安全、领域流程与命令语法分别由专门文件和机器交接维护。
- 名称搜索支持 Cn/Tw、字面包含、跨行名称和常见全角/半角标点，并新增批量名称、ID、路径解析和登记身份回传，减少重复打开 PVF 与无效二次搜索。
- 新增按需源 PVF 指纹路线；用户要求源文件前后不变时，会在修改前后核对同一组完整 SHA256。
- 原始读取、变更校验和预演提供明确的下一条命令。中文、参数和结构修改支持同文件联动、精确上下文、非重叠范围和严格数量绑定，未知字段或目标漂移会停止。
- 普通配置脚本扩大了只包含数字、英文和常见符号的受控修改范围；新增高风险文件与 worldmap 生命周期必须提交专用证明，既有高风险文件仍保持保护。
- 完善外置 profile、内容寻址备份、累计修改和测试客户端部署/恢复，避免刷新工作台丢配置或把旧增量覆盖到新客户端基线。
- 增加指令体积门禁、Skill/Agent 回归和三段发布门禁；真实 PVF、客户端、报告与本机状态继续排除在发布包外。

## 2.2.2

- 区分“用于查看的文本”和“可作为修改依据的原始 token”，并为展示文本误用与 Cn/Tw 编码不一致提供安全恢复路线。
- 新增显式累计修改链、外置 profile 和按 SHA256 复用的源版本备份；部署预览会阻止错误基线回退历史改动。
- 同文件多条文字改为一次处理，支持真实多行、准确计数、紧邻上下文和参数/结构联动；批量核验记录同步精简。
- 参数与结构改用原始 token 最小补丁，避免全文重编译破坏既有中文或 StringLink。
- `.str`、StringLink 显示文本、部分中文、无法编码字符、出现序号定位和未准确计数的批量仍保持阻断。

## 2.2.1

- 普通脚本中的单个完整中文名称或说明支持目标确认的 Cn/Tw 受控写入；预演会生成隔离临时输出并由独立解析器读回。
- 修复繁体 PVF 被错误按简体复核的问题，并保留旧 `verified-inline-cn` 变更集兼容。
- 源文件不覆盖、独立备份、输出 SHA256、字符串表保持和生成后复查继续作为硬要求；`.str`、StringLink、部分文字和批量中文当时仍不开放。

## 2.2.0

- 新增面向新手的通俗回复规范和真实命令型 Agent 快速路线，减少无目标扫描、帮助探测和重复知识加载。
- 增加受控的测试客户端 PVF 预览、部署与恢复；客户端资源写入仍不在授权范围。
- 增加技能解析、NUT 脚本观察、Tag 观察、统一依赖查询和机器可读完成边界。
- 收敛随包 Tag 与技能参数知识，只保留可执行的紧凑事实，外部研究材料和机器路径继续留在工作台外。
- 强化 PVF 内部路径、只读入口和中文语义读取保护；写入只由受控 `pvf-change apply` 暴露。
- 索引绑定完整 PVF SHA256；只读备用后端增加资源上限、路径攻击负控和合成回归。

## 2.1.1

- 修正 PVF 编译器排版规范化导致的生成后误判，在保留全文 SHA 的同时增加严格受限的 token 等价校验。

## 2.1.0

- 只读备用后端迁移为由固定 Node.js 直接执行的 TypeScript，不需要 npm、联网或构建。
- 备用模式只公开读取工具，并在分发层和 API 层同时阻止所有写入口。
- 增加资源上限、损坏文件失败关闭、LST 相对路径解析和大型 PVF 按需解码。
- 扩展 runtime、stdio、读写边界和真实多版本 PVF 差分回归。

## 2.0.0

- 工作台改为自包含：随包提供固定 Node.js、native PVF 后端、只读备用后端和统一 `workbench.bat` 入口。
- 知识包收口为安全规则、百科、词典、流程、任务卡和轻量路由，移除研究账本、来源正文和机器路径。
- 增加受控 change-set、预演、明确授权、独立输出、备份、生成后复查和清单记录。
- 增加内容分析、跨版本比较、依赖预览、客户端兼容矩阵和项目级 Agent Skill。
- 增加 runtime、知识、Agent 与便携发布门禁；native 无法加载时自动降级为严格只读。

## 1.1.0

- 早期公开版本。
